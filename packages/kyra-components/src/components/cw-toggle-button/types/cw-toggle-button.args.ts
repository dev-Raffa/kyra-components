export interface cwToggleButtonArgs {
    variant: 'default' | 'outiline' | 'icon'
    width: string;
    height: string;
    selectedBgColor: string;
    unselectedBgColor: string;
    selectedColor: string;
    unselectedColor: string;
}